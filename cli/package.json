{
  "name": "rosclit",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "cli": "Bun run index.ts",
    "build": "bun build ./index.ts --outfile cli --compile --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}